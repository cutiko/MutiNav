ext {
    navigation = [
            'featureA': 'cl.cutiko.featurea.FeatureAActivity.CUSTOM_ACTION',
            'featureB': 'cl.cutiko.featureb.FeatureBActivity.CUSTOM_ACTION',
            'featureC': 'cl.cutiko.featurev.FeatureCActivity.CUSTOM_ACTION'
    ]
}


buildscript {

    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.1'
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }

}

task clean(type: Delete) {
    delete rootProject.buildDir
}
